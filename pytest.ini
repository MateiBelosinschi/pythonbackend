[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short -m "not slow"
markers =
    slow: end-to-end tests that load the basic-pitch model (run with `pytest -m slow`)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
